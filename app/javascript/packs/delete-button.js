const deleteBtn = () => {
  const button = document.querySelector(".btn-delete");
  const article = document.querySelector(".my-product");
  button.addEventListener("click", (event) => {
    event.preventDefault();

  })
};
